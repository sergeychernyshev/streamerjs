---
"@streamerjs/streamerjs": minor
---

Remove the `create-scene` and `create-control-panel` commands, which the `create-streamerjs` package now provides as `npm create streamerjs scene` and `npm create streamerjs control-panel`
